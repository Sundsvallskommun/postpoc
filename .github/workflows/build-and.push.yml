name: Build and Push Docker image

on:
  workflow_call:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    strategy:
      fail-fast: false
      matrix:
        arch: [amd64, arm64]

    outputs:
      repo_owner: ${{ steps.repo_owner.outputs.owner_lc }}
      repo_name: ${{ steps.repo_name.outputs.name_lc }}
      version: ${{ steps.maven.outputs.version }}

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up Java (for Maven)
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '21'

      - name: Cache Maven repository
        uses: actions/cache@v4
        with:
          path: ~/.m2/repository
          key: m2-${{ runner.os }}-${{ hashFiles('**/pom.xml') }}
          restore-keys: |
            m2-${{ runner.os }}-

      - name: Maven package (host)
        run: mvn -B -DskipTests clean package

      - name: Read Maven version
        id: maven
        run: |
          VERSION=$(mvn -q -DforceStdout help:evaluate -Dexpression=project.version)
          echo "version=${VERSION}" >> $GITHUB_OUTPUT

      - name: Enable QEMU (cross-arch emulation)
        uses: docker/setup-qemu-action@v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.repository_owner }}
          password: ${{ secrets.GITHUB_TOKEN }}
          logout: false

      - name: Get lowercase repo owner
        id: repo_owner
        run: echo "owner_lc=${GITHUB_REPOSITORY_OWNER,,}" >> $GITHUB_OUTPUT

      - name: Get lowercase repo name
        id: repo_name
        shell: bash
        run: |
          repo_name="${GITHUB_REPOSITORY##*/}"
          echo "name_lc=${repo_name,,}" >> $GITHUB_OUTPUT

      - name: Build & push per-arch tags
        uses: docker/build-push-action@v6
        with:
          context: .
          push: true
          platforms: linux/${{ matrix.arch }}
          tags: |
            ghcr.io/${{ steps.repo_owner.outputs.owner_lc }}/${{ steps.repo_name.outputs.name_lc }}:${{ steps.maven.outputs.version }}-${{ matrix.arch }}
            ghcr.io/${{ steps.repo_owner.outputs.owner_lc }}/${{ steps.repo_name.outputs.name_lc }}:latest-${{ matrix.arch }}
          cache-from: type=gha
          cache-to: type=gha,mode=max
          provenance: false
          sbom: false
          labels: |
            org.opencontainers.image.source=${{ github.repository }}
            org.opencontainers.image.version=${{ steps.maven.outputs.version }}

  manifest:
    needs: build
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write

    steps:
      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.repository_owner }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Create multi-arch manifest for :<version>
        run: |
          docker buildx imagetools create \
            -t ghcr.io/${{ needs.build.outputs.repo_owner }}/${{ needs.build.outputs.repo_name }}:${{ needs.build.outputs.version }} \
            ghcr.io/${{ needs.build.outputs.repo_owner }}/${{ needs.build.outputs.repo_name }}:${{ needs.build.outputs.version }}-amd64 \
            ghcr.io/${{ needs.build.outputs.repo_owner }}/${{ needs.build.outputs.repo_name }}:${{ needs.build.outputs.version }}-arm64

      - name: Create multi-arch manifest for :latest
        if: ${{ github.ref == 'refs/heads/main' && !endsWith(needs.build.outputs.version, '-SNAPSHOT') }}
        run: |
          docker buildx imagetools create \
            -t ghcr.io/${{ needs.build.outputs.repo_owner }}/${{ needs.build.outputs.repo_name }}:latest \
            ghcr.io/${{ needs.build.outputs.repo_owner }}/${{ needs.build.outputs.repo_name }}:latest-amd64 \
            ghcr.io/${{ needs.build.outputs.repo_owner }}/${{ needs.build.outputs.repo_name }}:latest-arm64
